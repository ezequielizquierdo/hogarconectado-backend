# Plantilla para conectar el backend con MongoDB Atlas.
# Copiar como `.env.atlas` y completar únicamente en el entorno local.
# No agregar credenciales reales al repositorio.
MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/hogarconectado?retryWrites=true&w=majority
NODE_ENV=production
